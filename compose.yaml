services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: FEMBOY_DEV 
    working_dir: /workspace
    volumes:
      - .:/workspace
      - nuget-cache:/root/.nuget/packages
    tty: true
    stdin_open: true

volumes:
  nuget-cache:
